import React from 'react';
import {
  Switch,
  Route,
} from "react-router-dom";

export default () => {
  return (
    <Switch>
      {/* <Route path = "/" component = {} exact/> */}
    </Switch>
  );
}